# Only needed if frontend and backend are deployed separately.
# Leave unset for local dev — the Vite proxy in vite.config.js
# forwards /api requests to http://localhost:4000 automatically.
VITE_API_BASE=
